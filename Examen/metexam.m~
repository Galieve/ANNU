function [t,x] = metexam(f,intervalo,x0,N)
%METEXAM Summary of this function goes here
%   Detailed explanation goes here

h = (intervalo(2) - intervalo(1)) / N;
t = intervalo(1):h:intervalo(2);
x(:,1) = x0;

for i=1:N
   x(:,i+1) = x(i) + h/4*(f(ti,x(:,i)) 
end

